[build]
builder = "DOCKERFILE"
dockerfilePath = "vanmitra_backend/Dockerfile"
buildContext = "vanmitra_backend"

[deploy]
startCommand = "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
