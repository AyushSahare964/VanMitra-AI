import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('VanMitra-AI app starts', (WidgetTester tester) async {
    // Basic smoke test — full widget tests will be added later
    expect(1 + 1, equals(2));
  });
}
