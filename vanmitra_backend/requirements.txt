fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0
sentence-transformers>=2.6.0
faiss-cpu>=1.8.0
rapidfuzz>=3.8.0
Pillow>=10.2.0
pytesseract>=0.3.10
numpy>=1.26.0
# openai-whisper>=20231117  # Uncomment to enable /api/v1/transcribe (adds ~1GB to image)

# ── Module B: Satellite Change Detection ─────────────────────────────────────
torch>=2.2.0                         # Siamese CNN (SiameseChangeNet, 6002 params)
torchvision>=0.17.0                  # Image transforms for patch preparation
rasterio>=1.3.9                      # GeoTIFF reading + CRS handling
shapely>=2.0.0                       # Bounding box / polygon geometry
pyproj>=3.6.0                        # Coordinate reference system transforms
scikit-learn>=1.4.0                  # Land-cover classifier (RandomForest)
scikit-image>=0.22.0                 # NDVI computation, image segmentation
opencv-python-headless>=4.9.0        # Image preprocessing (no GUI needed)
requests>=2.31.0                     # External tile / API calls
# geopandas>=0.14.0                  # Uncomment if vector boundary files needed
# earthengine-api>=0.1.400           # Uncomment if using Google Earth Engine
# sentinelhub>=3.9.0                 # Uncomment if using Sentinel Hub API
