org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:ReservedCodeCacheSize=512m -Dfile.encoding=UTF-8
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
android.suppressUnsupportedCompileSdk=36
# Demote Kotlin JVM-target mismatch from ERROR → WARNING.
# Third-party plugins (cloud_functions, flutter_image_compress_common, etc.) ship
# with inconsistent Java/Kotlin JVM targets that we cannot control from root scripts
# due to AGP finalization timing.  WARNING keeps the build going; IGNORE silences it.
kotlin.jvm.target.validation.mode=WARNING
