class AppRadius {
  AppRadius._();

  /// 8px — Small chips, status badges, tiny indicators
  static const double sm = 8.0;

  /// 12px — List items, actionable rows, buttons, inputs
  static const double md = 12.0;

  /// 16px — Standard cards, hero banners, bottom sheets, dialogs
  static const double lg = 16.0;
}
