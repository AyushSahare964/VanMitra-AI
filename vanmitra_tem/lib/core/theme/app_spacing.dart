class AppSpacing {
  AppSpacing._();

  /// 4px — Icon-to-label gaps
  static const double xs = 4.0;

  /// 8px — Inline element gaps, tight stacking
  static const double sm = 8.0;

  /// 16px — Standard card padding, general section gaps
  static const double md = 16.0;

  /// 24px — Section-to-section spacing, major division breaks
  static const double lg = 24.0;

  /// 32px — Screen top/bottom margins, large hero padding
  static const double xl = 32.0;

  /// 40px — Extra-large hero breaks and list bottom allowances
  static const double xxl = 40.0;
}
